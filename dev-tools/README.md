# Development Tools

This folder keeps manual checks, debug pages, one-off migrations, backups, logs, and local quality tools out of the public application root.

Direct web access to this folder is blocked by `.htaccess`. Run scripts from the project root and review database-changing scripts before use.
